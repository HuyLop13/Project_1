header.header
    .container-fluid
        .row.align-items-center 
            .col-3 
                .inner-logo
                    a(href=`${prefixAdmin}/dashboard`) Xin Chào, ADMIN 
                    
            .col-9
                .text-right 
                    a(href=`${prefixAdmin}/my_account` class="btn btn-primary btn-sm mr-2") #{user.fullname}
                    a(href=`${prefixAdmin}/auth/logout` btn-logout class="btn btn-danger btn-sm") Đăng xuất